<<<<<<< SEARCH
    pub air_assist: bool,
    pub z_offset: f32,
    pub visible: bool,      // Output enabled?

    // Tabs / Bridges
=======
    pub air_assist: bool,
    pub z_offset: f32,
    pub visible: bool,      // Output enabled?
    #[serde(default = "default_show")]
    pub show: bool,         // Show in preview?

    // Tabs / Bridges
